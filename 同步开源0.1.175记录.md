# 同步开源 0.1.175 记录

## 一、同步信息

- 同步日期：2026-08-12
- 本地同步前提交：`231e766d0`
- 本地同步前版本：`0.1.173`
- 上游仓库：`Wei-Shaw/sub2api`
- 上游目标标签：`v0.1.175`
- 上游目标提交：`93c32fa1a2450351561abc46156d2e28cb5f74ca`
- 标签说明：上游本区间未发布独立的 `v0.1.174` 标签，本次直接对比 `v0.1.173..v0.1.175`。
- 备份分支：`backup/pre-sync-v0.1.175-20260812`
- 同步方式：将 `v0.1.173..v0.1.175` 的完整二进制差异以三方合并方式应用到当前二开分支，再逐项解决冲突。
- 上游差异规模：114 个文件，约 7319 行新增、351 行删除。
- 当前状态：同步完成，未提交、未推送、未部署。

## 二、上游更新内容

本次纳入上游 `v0.1.175` 的主要能力和修复：

1. 新增 Codex OAuth 设备指纹收敛，减少上游可见设备数和会话数，并补充 User-Agent 指纹校验。
2. 完善 OpenAI Responses、Chat Completions、WebSocket V2、图片以及 OAuth 路径的错误处理、TTFT、用量完整性和故障转移。
3. 新增上游实际响应模型计费，完善 service tier 成本、Responses 用量路径优先级和 Grok 缺失用量计费。
4. 修复 OpenAI HTML 403 被误判为账号级故障、确定性 400 错误转发和连接池认证重试。
5. 完善 Cyber Policy 审计作用域、WebSocket 审计日志和最新轮次去重。
6. 完善 API Key 配额、限流和有效期参数校验，拒绝负数、非有限数值及非法过期天数。
7. 完善大文件备份分卷上传与恢复、Gemini 工具 Schema、运维内存展示及用量表请求 ID 展示。

## 三、冲突与二开兼容

本次三方应用共处理 5 个显式冲突。处理原则为：现有二开业务行为优先，上游修复可兼容时合并，会改变现有业务边界时保留二开实现。

### 3.1 README 文件

- `README.md`、`README_CN.md`、`README_JA.md` 保留当前项目的居中排版。
- Go 版本徽章同步为上游使用的 `1.26.5`。

### 3.2 API Key 服务

- 保留二开 `Create -> create(..., allowHiddenGroup)` 的创建路径。
- 普通用户仍不能创建隐藏分组的密钥；可信对话站自动建钥仍可以绑定隐藏免费分组。
- 合入上游 API Key 配额、5 小时/1 天/7 天限流及过期天数的非负、有限值校验。

### 3.3 OpenAI 账号调度

- 保留指定模型优先使用指定账号的二开路由。
- 指定账号不可用时仍回退至符合条件的普通账号。
- 专属账号不会接收其他模型的请求。
- 合入上游账号筛选诊断统计和利润控制预检查，无可用账号时能返回更具体的排除原因。

## 四、二开能力保护检查

已逐项检查并保留以下现有功能：

- 隐藏分组、免费分组、每日免费额度和免费分组余额豁免边界。
- 仅允许对话站调用，以及对话站自动创建隐藏密钥。
- OpenAI 模型专属账号路由、专属账号隔离和不可用时回退。
- Cyber Policy 输入摘要和哈希入库；同时纳入上游修复，审计事件遵循内容审核的分组和模型作用域。
- Responses `input_tokens` 本地处理、Grok 参数清洗和运维健康度噪声排除。
- LinkCode 门户的 QQ 群、Telegram、对话站、定价和教程等二开字段及界面。

## 五、数据库迁移说明

对比 `v0.1.173..v0.1.175` 后确认，本次上游没有新增数据库迁移文件，也没有需要手工执行的 SQL。

Docker 发布仍会按现有流程执行应用启动检查。虽然本次无新迁移，正式发布前仍建议备份 PostgreSQL。

## 六、版本处理

上游 `v0.1.175` 标签内的 `backend/cmd/server/VERSION` 仍为 `0.1.173`。本项目服务器构建不是上游精确标签构建，会回退读取该文件，因此已显式更新为 `0.1.175`，避免 Docker 构建产物仍显示旧版本。

## 七、验证结果

以下检查已通过：

```text
go test ./... -run '^$' -count=1
go test -tags=unit ./... -count=1
go test -tags=unit ./internal/server -run TestAPIContracts -count=1
corepack pnpm typecheck
corepack pnpm exec vitest run
NODE_OPTIONS=--max-old-space-size=4096 corepack pnpm build
CGO_ENABLED=0 go build -tags embed -trimpath ./cmd/server
git diff --check
```

前端全量 Vitest 结果：

```text
Test Files  224 passed (224)
Tests       1558 passed (1558)
```

另外已完成高风险二开定向回归，覆盖模型专属路由优先级、不可用回退、专属账号隔离、免费额度计费、Cyber Policy 摘要/哈希、Responses 空完成事件故障转移和上游响应模型计费。

## 八、发布与回退注意事项

1. 当前改动按要求保持未提交、未推送、未部署，生产服务器不会自动获得本次同步内容。
2. 同步前代码可通过提交 `231e766d0` 或备份分支 `backup/pre-sync-v0.1.175-20260812` 定位。
3. 提交前应再次确认差异仅包含本次上游同步、兼容修正和本文档。
4. 正式发布前建议先在测试环境构建、备份数据库，并保留当前生产镜像以便快速回退。

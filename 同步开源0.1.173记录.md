# 同步开源 0.1.173 记录

## 一、同步信息

- 同步日期：2026-08-11
- 本地同步前提交：`9862c94a8`
- 本地同步前版本：`0.1.172`
- 上游仓库：`Wei-Shaw/sub2api`
- 上游目标标签：`v0.1.173`
- 上游目标提交：`29009f0b2ea14edf3b11ae2564fb617ff91a03b4`
- 备份分支：`backup/pre-sync-v0.1.173-20260811`
- 同步方式：将 `v0.1.172..v0.1.173` 的完整二进制差异以三方合并方式应用到当前二开分支，再逐项解决冲突。
- 当前状态：同步完成，未提交、未推送、未部署生产。

## 二、上游更新内容

本次纳入上游 `v0.1.173` 的主要功能和修复：

1. 新增渠道监控 V2，包括聚合、趋势、排行、配置、权限、隐私默认值和渐进式历史回填。
2. 新增邮箱域名注册额度开关及相应注册流程校验。
3. 完善 Grok 视频按模型计价、搜索计价、语音计价、OAuth、额度探测、限流恢复和错误分类。
4. 修复 Gemini 生图输出计量与计费。
5. 新增 OpenAI routing hints，并完善搜索附加费、WebSocket、Responses、图片和 Messages 转发逻辑。
6. 增强凭据脱敏、Redis OAuth 会话回退、账号调度阈值及网关热路径处理。
7. 同步前后端类型、国际化、测试、配置示例、Ent 代码和 Wire 依赖注入代码。

## 三、二开兼容处理

本次三方应用共处理 16 处显式冲突。处理原则为：二开业务能力优先保留；上游能力能够并存时做字段、请求和调度逻辑合并；无法安全替换二开逻辑时舍弃对应上游覆盖。

### 3.1 分组与密钥

- 保留隐藏分组、免费分组、每日免费额度和仅对话站调用配置。
- 保留分组 IP ACL 及请求来源校验。
- 保留 OpenAI 模型专属账号路由和不可用时的兼容回退行为。
- `APIKeyAuthGroupSnapshot` 同时保留本地免费/IP 字段，并加入上游视频、搜索和语音计价字段。
- API 密钥认证缓存快照版本更新为 `v20`，避免旧缓存缺少新增字段。
- 分组管理创建与编辑请求同时提交免费额度、IP ACL 和 `video_model_prices`，防止任一侧字段被覆盖。

### 3.2 网关与模型

- 保留 Grok 4.5 不支持参数清洗，同时合入 Grok 4.20 的 `logprobs` 清洗。
- 保留提示词审计路由覆盖和现有 Cyber Policy 记录逻辑。
- 合入 OpenAI routing hints、Grok 搜索/语音/视频计费和 Gemini 生图计费修复。
- 保留现有对话站免费分组、余额豁免边界和密钥自动创建行为。

### 3.3 门户与管理端

- 保留 LinkCode 门户的 QQ 群、对话站、定价、教程、Telegram 和返利配置。
- 保留对话站 SSO 打开逻辑及现有门户布局。
- 门户文档链接和站点 Logo 继续通过 `sanitizeUrl` 清洗，并兼容上游安全回归测试。
- 分组页面同时展示和保存二开的免费/IP 配置与上游视频模型价格配置。

### 3.4 生成代码

- 执行 `go generate ./ent`，按合并后的 Ent Schema 重建实体、查询、迁移和运行时代码。
- 执行 `go generate ./cmd/server`，按合并后的依赖图重建 Wire 代码。

## 四、数据库迁移说明

本次新增上游迁移：

- `194_channel_monitor_v2.sql` 至 `206_channel_monitor_v2_privacy_defaults.sql`
- `217_group_video_model_prices.sql`
- `218_group_audio_voice_pricing.sql`
- `219_group_search_price_per_1k.sql`
- `220_clear_non_grok_video_generation_config.sql`

迁移执行器使用完整文件名作为迁移主键，而不是只使用数字前缀。仓库历史中也存在相同数字前缀但不同文件名的迁移，因此保留上游原文件名；同时，上游迁移校验和兼容规则依赖这些名称，不进行重编号。

Docker 更新发布时，后端启动会按现有迁移执行器自动应用尚未执行的迁移。正式发布前仍应备份数据库，并先在测试环境验证迁移耗时和渠道监控 V2 的资源使用情况。

## 五、版本处理

上游 `v0.1.173` 标签内的 `backend/cmd/server/VERSION` 仍为 `0.1.172`。本项目的服务器构建不是上游精确标签构建，会读取该版本文件，因此按既有同步约定显式设置为 `0.1.173`，确保构建产物和管理端显示与同步目标一致。

## 六、验证结果

以下检查已通过：

```text
go test -tags=unit ./...
corepack pnpm@9.15.9 typecheck
NODE_OPTIONS=--max-old-space-size=4096 corepack pnpm@9.15.9 build
CGO_ENABLED=0 go build -tags embed -trimpath ./cmd/server
```

前端全量 Vitest：

```text
Test Files  222 passed (222)
Tests       1549 passed (1549)
```

部署配置检查：

```text
docker compose security test passed
docker runtime resources test passed
Caddyfile preserves backend cache policy, SSE streaming, and non-SSE compression
```

另外已执行二开定向回归，覆盖免费分组、仅对话站调用、隐藏分组、模型专属路由、Grok、视频、兑换码、邀请返利、公开设置、API 密钥认证和 Cyber Policy。

## 七、发布注意事项

1. 当前改动按要求保持未提交、未推送，生产服务器不会自动获得本次同步内容。
2. 提交前应再次确认工作区差异只包含本次同步、兼容修改和本文档。
3. 发布前先备份 PostgreSQL，并保留同步前提交 `9862c94a8` 和备份分支用于代码回退。
4. 代码回退不会自动回滚已经执行的数据库迁移；如需完整回退，应结合数据库备份处理。
5. 渠道监控 V2 默认保持安全配置，生产启用前应观察数据库负载和历史回填进度。

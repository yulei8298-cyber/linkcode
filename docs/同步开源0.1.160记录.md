# 同步开源 0.1.160 记录

## 目标与范围

- 本地基线：`0.1.158`（提交 `a6c0821f2`）。
- 上游范围：Wei-Shaw/sub2api `v0.1.158` 至 `v0.1.160`。
- 目标版本：`0.1.160`。
- 本次完成同步、二开兼容和验证；不提交、不推送。

本地与上游 Git 历史不连续，不能直接 merge 上游标签。本次先暂存未提交
二开修复，再以 `v0.1.158..v0.1.160` 三方补丁同步，逐项处理冲突后恢复本地
修改。

## 已同步的上游能力

1. OpenAI 兼容提示词输入审计：后端协调器、异步任务与事件记录、管理端配置/事件
   页面、路由和 Wire 注入。
2. Grok 媒体请求图片引用格式归一化、不可用账号隔离、调度缓存修复，以及 Responses
   function tool 免费缓存支持。
3. OpenAI API Key 账号的 `/alpha/search` 调度恢复和 404/405 故障切换。
4. 被动 `image_gen` namespace 的显式意图判断，避免错误要求 Responses 能力。
5. API Key ACL、审计日志和会话绑定统一使用“信任反代传递的客户端 IP”开关。
6. S3 备份保存接入 step-up TOTP，Stripe 前端依赖按需加载，账号名称可跳转上游站点。

上游附带的 `openspec/changes/add-openai-compatible-prompt-audit` 源码快照和研发
留档未纳入。本目录不参与运行、迁移或构建，且包含大量补丁与归档文件；实际提示词
审计的运行代码、前端、测试和迁移均已同步。

## 二开兼容处理

1. 保留对话站首个无密钥请求自动创建隐藏免费密钥、对话站专用分组、免费分组每日
   额度及余额不足豁免。
2. API Key 鉴权合并上游 IP 统一逻辑：分组 ACL 仍先校验，密钥 ACL 后校验；两者都
   使用 `GetSecurityClientIP`，同时保留本地对话站密钥和免费额度限制。
3. 保留分组 IP 白名单/黑名单、Grok 对话/生图/视频兼容、LobeHub 相关二开和页面展示。
4. 恢复同步前未提交的修复：HTTP(S) 自定义菜单改为新标签页打开；删除分组会清理
   系统及各认证来源的默认订阅引用；系统设置保存前自动过滤已经删除的订阅分组。
5. 上游提示词审计路由覆盖测试补充本地 Antigravity 路径：`/antigravity/messages`
   进入现有审计链，`/antigravity/messages/count_tokens` 明确标为无模型调用。

服务显示版本更新为 `0.1.160`。上游 `v0.1.160` 标签内的 `VERSION` 文件仍为
`0.1.159`，但本地此前同步约定以目标标签作为显示版本，本次保持该规则。

## 迁移编号处理

本地已占用 `181_audit_logs.sql` 与 `182_group_duplicate_operation_id.sql`，不能覆盖。
上游提示词审计迁移按内容原样保留，仅顺延文件名前缀：

| 上游文件 | 本地同步后文件 |
| --- | --- |
| `181_prompt_audit.sql` | `183_prompt_audit.sql` |
| `182_prompt_audit_full_prompt.sql` | `184_prompt_audit_full_prompt.sql` |

迁移 SQL 已逐字比对；集成测试读取的文件名也已同步更新。升级时迁移器会按文件名顺序
执行 `183`、`184`，不会影响已经记录的本地 `181`、`182`。

## 验证

已通过：

- `cd frontend && pnpm typecheck`
- `cd frontend && pnpm exec vitest run src/components/layout/__tests__/AppSidebar.spec.ts src/views/admin/__tests__/SettingsView.spec.ts src/features/prompt-audit`（58 项）
- `cd frontend && pnpm build`
- `cd backend && go test -tags=unit ./internal/pkg/antigravity ./internal/server/routes ./internal/securityaudit ./internal/server/middleware ./internal/service`
- `cd backend && go build -tags=embed ./cmd/server`
- 迁移文件与上游内容比对、`git diff --check` 和未解决冲突检查。

完整 `go test -tags=unit ./...` 已执行。同步前即存在的 API 契约快照与本地二开输出
不一致，仍有以下夹具失败：

- `GET /api/v1/groups/available`：夹具未包含本地分组 ACL 字段。
- `GET /api/v1/admin/settings` 及其 OAuth 默认配置回退场景：夹具未包含本地对话站/定价
  等设置字段及 LinkCode 默认展示值。

这些是测试期望值滞后，不是本次同步导致的服务端编译或运行错误；本次新增的提示词
审计路由覆盖问题已修复并通过定向测试。

## 提交状态

本记录和同步代码均保留在工作区；未创建 Git 提交，未推送远端。

# 同步开源版本 0.2.4 并兼容二开记录

## 1. 目标与基线

- 上游仓库：`Wei-Shaw/sub2api`
- 目标版本：`v0.2.4`，标签提交 `98d86915b`（版本提交的父提交为 `5de5e2bed`）。
- 本地项目：`linkcode`，同步前基线 `6949f000c`，内嵌版本 `0.2.1`。
- 上游增量：覆盖 `v0.2.2`、`v0.2.3`、`v0.2.4`，约 499 个文件；本次工作区实际变更以 `git diff` 为准。

## 2. 合并策略

本地历次同步采用手工重放，上游标签与本地 `main` 没有可直接使用的合并基线。因此先保存未提交二开，生成 `v0.2.1..v0.2.4` 二进制补丁，再在本地工作树应用；冲突按“二开行为优先、上游能力补入”的原则处理。同步未执行提交、推送、部署、数据库迁移或 DNS 切换。

原有未提交内容已保留：部署请求体限制与 HTTP/2 参数、峰谷定价实现及测试、迁移排查记忆文件。

## 3. 关键冲突与兼容处理

| 范围 | 处理结果 |
| --- | --- |
| `models_list_config` 与上游 `model_allowlist` | Ent 与服务层保留旧 `models_list_config`，新增 `model_allowlist`；模型广场继续读取旧字段，网关白名单使用新字段。 |
| 迁移 235/236 | 改为新增并回填 `model_allowlist`，只复制 `enabled/models`；保留 `plaza_enabled/plaza_models` 和旧列，避免模型广场配置丢失。 |
| 模型广场与人民币定价 | 保留主题卡片、真实分组模型、可编辑基础价格、国内官方人民币价格和 Kimi K2.7 定价。 |
| 网关/账号缓存 | 接入上游白名单中间件、模型列表能力和 WebSocket 后续 turn 校验；认证快照同时保存两套配置并升级版本号。 |
| 路由与部署配置 | 保留 LinkCode 的门户、免费/隐藏分组、IP ACL、支付、无限画布、SSO、对象存储和部署参数。 |

## 4. 吸收的上游能力

- MiniMax 平台及账号、分组调度、渠道和额度监控。
- OpenAI Image 2.5、生图 OAuth 模型选择修复和模型列表增强。
- Grok 媒体资格控制及参数兼容修复。
- 长流 HTTP/2 PING 保活、客户端断开时先取消上游流。
- OpenAI 账号预估周消耗、渠道监控 V2 用户排行显隐。
- 支付帮助 Markdown 净化、自定义页面链接按钮拖拽、Apple 容器自定义子网。
- 系统日志上限、渠道缓存集群广播、持久化冷却与多项代理/注册/账号修复。

## 5. 数据库迁移

新增上游迁移 235、236、237（模型白名单修复、MiniMax 平台），均保留在 `backend/migrations`。235/236 已按二开双列结构调整，执行前仍应完成数据库备份和恢复演练；本次未执行迁移。现有二开迁移编号和非事务索引迁移保持不变。

## 6. 验证结果

- 前端 `pnpm run typecheck`：通过。
- 前端模型白名单定向用例（16 项）：通过。
- 前端全量用例：275/276 个测试文件、2024/2025 个测试通过；唯一失败是既有 `ChannelMonitorView.grok.spec.ts` 仍断言 8 个 provider，而 0.2.4 已新增 MiniMax，实际为 9 个 provider。
- 后端 `go test ./...`：通过。
- 后端定向 HTTP 用例在当前沙箱无法监听 `httptest.NewServer` 本机端口（`operation not permitted`），需在可监听环境复跑。
- `git diff --check`：通过。
- 完整前端构建、生产级数据库/Redis 集成测试，本次未执行。

## 7. 当前状态与发布建议

当前仅完成工作区同步，未提交、未推送、未部署，也未执行数据库迁移。发布前建议在备份数据库上演练 235/236/237，完成后端全量测试、前端构建和灰度验证，再单独提交同步结果。

# 同步开源 0.1.177 兼容二开记录

## 一、同步信息

- 同步日期：2026-08-16
- 本地仓库：`linkcode`
- 当前分支：`main`
- 同步前本地提交：`c33218b216a73648cf0dceadbdf16079c7ead450`
- 同步前版本：`0.1.175`
- 上游仓库：`Wei-Shaw/sub2api`
- 上游目标标签：`v0.1.177`
- 上游目标提交：`4ad52642a1548c46533f458a60d30c192026795e`
- 上游 `v0.1.175..v0.1.177` 差异规模：166 个文件，8188 行新增，641 行删除
- 同步前回退分支：`backup/pre-sync-v0.1.177-20260816`
- 本次状态：未提交、未推送、未部署

## 二、同步原则

1. 以 `v0.1.175..v0.1.177` 的上游差异为同步来源。
2. 保留当前 LinkCode 二开能力；冲突时优先兼容二开，不让上游改动覆盖生产功能。
3. 已发布迁移不覆盖、不修改；上游迁移与本地编号冲突时顺延编号。
4. Ent 生成代码以合并后的 Schema 重新生成，不长期手工维护不一致的生成文件。
5. 本次只修改本地工作区，不执行提交、推送、服务器部署或数据库迁移。

## 三、引入的主要上游能力

- 分组逐模型定价及分组长上下文阶梯计费开关。
- 分组用量按日汇总，新增日汇总表、汇聚状态和时区兼容迁移。
- Codex remote compaction v2、会话级 beta 请求头及 `x-codex-turn-state` 回传。
- Codex 跨账号回合状态回显保护，压缩探测改用原生 remote compaction v2。
- Codex 指纹收敛改为 opt-in，避免未显式配置的账号被静默改写客户端标识。
- Grok 长上下文计费和带版本号媒体模型计费修复。
- Grok 4.6、JWT 订阅档位识别、原生 `/x_search` 及相关 API/前端测试更新。
- 渠道缓存失效、Responses 探测修复、账号刷新偏好修复及前端依赖/界面更新。

## 四、冲突处理

### 1. Ent 生成文件

已将合并后的 `backend/ent/schema/group.go` 作为唯一数据模型来源，重新运行 Ent 生成器：

```bash
cd backend/ent
go run -mod=mod entgo.io/ent/cmd/ent generate \
  --feature sql/upsert,intercept,sql/execquery,sql/lock \
  --idtype int64 ./schema
```

最终生成代码同时保留：

- 二开字段：隐藏分组、免费分组、每日免费额度、仅对话站调用。
- 上游字段：`long_context_pricing_enabled`、`model_pricing`。

此前混用两套生成代码会导致 `GroupMutation` 缺少字段方法并在 Docker 构建阶段失败，本次已统一重生成修复。

### 2. 分组管理后端

- `admin_service.go`、`admin_group.go` 同时保留免费分组、余额为零时的免费额度逻辑、对话站限制、IP ACL、模型路由和二开字段。
- 合入上游逐模型定价标准化、长上下文开关和渠道缓存失效逻辑。
- `group_handler.go` 同时接收/返回本地免费字段、IP ACL、上游长上下文开关和逐模型定价字段。
- 分组用量统计接入上游按日汇总逻辑。

### 3. 前端分组页面

- `frontend/src/types/index.ts` 保留本地免费/IP ACL/对话站字段，并加入长上下文和逐模型定价类型。
- `GroupsView.vue` 保留免费分组管理能力，加入上游逐模型定价、长上下文开关和昨日用量展示。
- 创建、编辑请求同时提交双方字段，避免页面保存时丢失二开配置。

### 4. 其他二开能力

本次同步过程中保留以下已上线能力：

- LinkCode 品牌、QQ 群 `1025176993`、对话站入口和门户二开页面。
- 隐藏分组、免费分组、每日免费额度、免费分组余额为零时继续调用。
- 对话站无密钥首聊自动创建隐藏免费分组密钥。
- 指定模型专属账号路由、专属账号不可用时回退普通账号、账号模型隔离。
- Cyber Policy 输入摘要和哈希记录、提示词审计及入口拒绝统计。
- 生图结果转存对象存储并统一返回本站域名。
- 兑换码成本金额、历史兑换码档位返利及中文错误提示。
- Grok 对话、生图、视频和客户端兼容路由。

## 五、迁移兼容

本地已有 `221_mark_legacy_responses_input_tokens_errors.sql`，上游新增迁移原本使用 `221/222/223`，因此未让两个 `221` 并列，顺延为：

| 上游文件 | 本地同步后文件 |
| --- | --- |
| `221_group_model_pricing.sql` | `222_group_model_pricing.sql` |
| `222_group_usage_daily_rollups.sql` | `223_group_usage_daily_rollups.sql` |
| `223_group_usage_rollup_timezone.sql` | `224_group_usage_rollup_timezone.sql` |

已有本地 `221_mark_legacy_responses_input_tokens_errors.sql` 保持不变。迁移器按完整文件名和校验和记录，顺延后可在已有生产数据库上继续执行，不会覆盖已经应用的本地迁移。

## 六、版本处理

- `backend/cmd/server/VERSION` 已设置为 `0.1.177`。
- 未修改已有业务版本之外的发布流程。
- 当前保留回退分支 `backup/pre-sync-v0.1.177-20260816`，用于对比同步前工作区。

## 七、验证结果

已通过：

```text
go run ... ent generate ... ./schema
go test ./... -run '^$' -count=1
go test ./migrations -count=1
go test ./internal/service -run 'Test(ModelPricing|Group|OpenAI|Grok)' -count=1
corepack pnpm typecheck
git diff --check
```

说明：本次完成了后端编译型检查和受影响服务/迁移测试，前端完成 TypeScript 类型检查；尚未执行完整前端生产构建、Docker 镜像构建、生产数据库迁移和线上发布。

## 八、最终状态

- 同步代码已保留在当前工作区。
- 冲突文件已处理，没有残留 Git 冲突标记。
- 未创建本次同步提交。
- 未推送远端。
- 未部署服务器。
- 发布前建议先在测试环境执行数据库迁移并构建 Docker 镜像，再安排生产滚动更新。

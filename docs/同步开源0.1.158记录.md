# 同步开源 0.1.158 记录

## 目标与范围

- 本地基线：`0.1.156`（提交 `69707fc`）。
- 上游范围：Wei-Shaw/sub2api `v0.1.156` 至 `v0.1.158`。
- 目标版本：`0.1.158`。
- 本次仅完成代码同步和验证，不提交、不推送。

本地与上游 Git 历史不连续，无法直接执行普通 merge，因此使用以上游
`v0.1.156 -> v0.1.158` 的三方补丁为基础，再逐项处理冲突。

## 保留的本地二开

1. 对话站首个无密钥请求的可信来源校验、自动创建隐藏密钥和响应头回传。
2. 分组隐藏、免费、每日免费额度、仅对话站调用，以及免费分组余额不足豁免。
3. 分组 IP 白名单/黑名单及其读取、水合和鉴权检查。
4. LobeHub 单点登录、公开定价路由、首页/API 密钥页的本地展示调整。
5. Grok 对话、生图、视频及 Codex/Claude 客户端配置兼容。

## 上游兼容处理

- 接入异步图片任务读取接口；任务查询、用量和密钥账单接口只做鉴权，不因余额或额度耗尽而拒绝。
- 接入管理操作审计、步进式二次验证、账号上游计费探测、渠道和用户批量管理等 `0.1.157`/`0.1.158` 更新。
- 重新生成 Ent 模型和 Wire 依赖注入代码，确保本地免费分组字段与上游
  `duplicate_operation_id` 同时生效。
- 管理员复制分组时，额外复制隐藏、免费、每日免费额度、仅对话站和 IP ACL 设置；复制后仍保持上游的账号绑定、优先级和调度事件原子性。
- Grok 配置弹窗采用上游的 OpenAI/Claude/Codex 分流，同时保留 `/v1` API 基址兼容。

## 迁移编号处理

本地已上线 `177_chat_station_free_groups.sql`。上游新增迁移也从 `177` 开始，
若保持原编号会导致迁移顺序和 `schema_migrations` 记录产生歧义。因此只顺延上游新增文件：

| 上游文件 | 本地同步后文件 |
| --- | --- |
| `177_add_subscription_plan_currency.sql` | `178_add_subscription_plan_currency.sql` |
| `178_channel_image_input_price.sql` | `179_channel_image_input_price.sql` |
| `179_usage_log_image_input_tokens.sql` | `180_usage_log_image_input_tokens.sql` |
| `180_audit_logs.sql` | `181_audit_logs.sql` |
| `181_group_duplicate_operation_id.sql` | `182_group_duplicate_operation_id.sql` |

迁移器按完整文件名排序并记录文件名，因此该处理不会修改已经执行过的
`177_chat_station_free_groups.sql`，升级时会继续按顺序执行新的五项迁移。

## 验证

已完成：

- `cd backend && go generate ./ent`
- `cd backend && go generate ./cmd/server`
- `cd backend && go test -tags=unit ./...`
- `cd frontend && pnpm typecheck`
- `cd frontend && pnpm test:run`
- `cd frontend && pnpm build`
- `cd backend && go clean -cache && go build -tags embed ./cmd/server`
- `git diff --check`、`git diff --cached --check` 和未解决冲突检查。

前端测试输出含既有 jsdom/router-link 警告，但测试命令以成功状态结束。

## 提交状态

本记录随同步修改保留在工作区；本次未创建 Git 提交，也未推送远端。

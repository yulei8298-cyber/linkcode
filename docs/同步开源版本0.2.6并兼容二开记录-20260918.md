# 同步官方版本 0.2.6 并兼容 LinkCode 二开记录

## 1. 来源、基线与范围

- 官方仓库：`Wei-Shaw/sub2api`。
- 官方输入固定为作者 tag 对象：
  - `v0.2.4^{}`：`5de5e2bed035d43591a2e10e51f420ef6a84eb98`
  - `v0.2.6^{}`：`49a39b6dc1abed30fd227611e8af1108bc427610`
- 本次实际增量：官方 `v0.2.4..v0.2.6`，约 529 个文件。
- 本地基线：`7d4a745b4a6c3b669b09226ee79685d2633739af`，相对 `origin/main` 领先的未推送二开提交保留。
- 版本：`backend/cmd/server/VERSION` 已更新为 `0.2.6`。
- `zcj-ui/sub2api-plus` fork 仅用于找回作者仓库曾撤回的 tag 对象和比对范围；本次代码重放没有使用 `fork-v0.2.6`，也没有引入 fork 私有提交、CN 定制、429 guard 或 Codex fingerprint 私有迁移。

## 2. 兼容策略与安全边界

工作在隔离 worktree `/private/tmp/linkcode-official-026` 完成，基于本地 HEAD 逐文件三方重放官方增量：非冲突改动吸收，重叠区域本地二开优先，再按函数/代码块补入官方能力。未使用普通 merge 覆盖二开，未 reset、clean、强覆盖；已将隔离结果应用回主工作树并提交推送。

同步阶段没有执行数据库迁移、DNS 或 OVH 生产网络操作；发布动作在后续发布记录中单独登记。

## 3. 保留的 LinkCode 二开

- IntelCheck 全链路：`unverified`、题目/轮次/结果实体、逻辑题单位修复、结构/绘图门禁、motion 预览和 OVH 发布编排。
- 模型广场真实分组模型、主题卡片、基础价格编辑、国内官方人民币价格和 CNY 展示。
- LobeHub SSO、门户、免费/隐藏分组、IP ACL、支付、对象存储图片转存和审计链路。
- 无限画布、部署端口/代理、motion 容器及其 LinkCode 参数。
- 本地未推送 HEAD 及既有迁移历史；官方迁移与本地 IntelCheck 迁移同名编号共存，不重命名、不删除。

## 4. 吸收的官方 0.2.6 能力

- OpenCode Go 平台与模型路由，包括 DeepSeek Flash、Grok、MiniMax 协议选择和账号测试映射。
- OpenAI 图片直调、模型映射、图片流和失败回退增强；图片对象存储/本地转存行为保留。
- Codex ticket 生命周期、harvest/注入能力及设置接口，默认保持关闭，不自动启用生产 ticket fail-closed。
- Gemini 原生响应信号、Responses/Anthropic 工具与媒体转换、OpenAI Responses bridge、请求体与流错误处理。
- 账号、订阅、兑换码、退款、配额、审计、后台批量操作、代理凭据更新和前端对应视图。
- 官方迁移新增：
  - `238_opencode_go_platform.sql`
  - `238_purge_unlimited_user_platform_quotas.sql`
  - 对应迁移测试

官方 238 与本地 IntelCheck 的 238 文件均保留；同步阶段不执行迁移，也不清理 quota。迁移在新对话站和 OVH 正式发布阶段按发布脚本执行。

## 5.1 发布前配置修复

- OpenCode Go 渠道监控校验已补齐，仅允许 quota 模式并区分 Zen/Go 账号能力。
- Codex 直调图片 SSE 已避免 raw/transformed 双重输出。
- 所有 Compose 变体补齐 Codex ticket 环境变量、IntelCheck motion sidecar 和 SSO disabled 展开路径。
- 新增 Compose 边界回归脚本，覆盖 compact 默认模型、ticket 透传、motion 服务和关闭 SSO 场景。

## 5. 冲突取舍与明确排除项

| 范围 | 处理 |
| --- | --- |
| 网关/图片/Responses | 官方能力按函数块引入；保留本地计费、审计、对象存储、模型广场和门户入口。 |
| WS/路由 | 保留本地 WS ingress、回退、审计、计费和代理行为；官方新增协议能力仅在不破坏本地路由的范围内合入。 |
| Ent | 重新生成 Ent，保留 IntelCheck 实体与官方 `user_platform_quota`/渠道监控变更。 |
| 迁移 | 只保留官方 238 文件和本地迁移历史，均未执行；没有 fork 私有 227–231 Codex fingerprint 迁移。 |
| fork 私有提交 | 明确排除 `d0cbc104`、`1d8e0e717` 等 fork 私有 guard、CN 定制和 Codex fingerprint 私有生命周期/迁移内容。 |
| 默认行为 | ticket 默认关闭；不改变生产默认模型；不启用 fail-closed ticket；不改变 LinkCode SSO、图片、审计或门户开关。 |

## 6. 验证结果

- 官方 Go 1.27.0 工具链（本机缓存）下：`go generate ./ent` 通过。
- 官方 Go 1.27.0 工具链下：`go test -run '^$' ./...` 全量编译通过。
- 定向测试通过：
  - OpenCode Go 账号测试与协议/模型映射；
  - OpenCode gateway mapping；
  - Codex ticket 注入、过期、长度、禁用、harvest 和状态探测。
- 图片直调定向测试中，涉及 `httptest.NewServer` 的白名单测试受当前沙箱限制：`listen tcp6 [::1]:0: bind: operation not permitted`；已执行的纯路由/映射测试通过，监听测试需在可监听环境复跑。
- 前端 typecheck/build 仍需在安装 `frontend/node_modules` 后执行；本轮没有擅自安装依赖。
- `git diff --check`：通过。
- `git ls-files -u`：0。

## 7. 新对话站发布结果（2026-09-18）

- 已提交并 push：`314e9f7b8`，随后补充前端特性开关兼容修复 `1b8800ab3`。
- 已发布到新对话站测试栈：`/opt/linkcode-test-0.1.172`；OVH 正式服务器、DNS 和生产线路未修改。
- 发布前源码备份：`/opt/linkcode-test-backups/pre-314e9f7b8/source-before-314e9f7b8.tar.gz`。
- 发布前 PostgreSQL 备份：`/opt/linkcode-test-backups/pre-314e9f7b8/postgres-before-314e9f7b8.sql.gz`。
- app 与 IntelCheck motion 容器均 `healthy`、重启次数 `0`；公网 `/health` 返回 `{"status":"ok"}`；运行版本为 `Sub2API 0.2.6`。
- 已执行并登记官方两个 238 迁移及本地 238–240；未执行 fork 私有迁移。
- `user_platform_quotas` 三档额度全为空的行由 `2146` 条清理为 `0` 条；用户数保持 `449`，用量记录保持 `1046243`。
- 发布后的镜像保留回滚 tag：`linkcode-test-0.1.172:rollback-314e9f7b8` 与对应 motion 镜像 tag。

## 8. 后续风险与未执行项

1. 图片/WS 中依赖 `httptest.NewServer` 的测试仍需在允许本机监听的环境复跑。
2. ticket 默认关闭；对象存储、SSO、审计和 OVH 正式发布配置未被本次新对话站发布改变。
3. 前端构建已在远端 Docker 构建阶段通过；本地工作区仍可再运行完整前端测试作为开发验证。

## 9. OVH 正式服务器发布结果（2026-09-19）

- 已在 OVH 正式服务器 `40.160.137.138` 完成发布，源码从旧提交 `6c59d6b960107ecf48f82389cc5d8b11d057183a` 更新到 `3bc5ae9d0b315380fcfe0dbcb733f41fc0389bab`。
- 发布脚本完成 motion sidecar 正向/负向 smoke、应用镜像构建、容器切换和迁移校验；应用 `sub2api` 与 `sub2api-intel-check-motion` 均为 `healthy`，重启次数均为 `0`，应用直连 `/health` 返回 `{"status":"ok"}`。
- 已执行官方 238 迁移、本地 IntelCheck 238–240 迁移；数据库最新迁移为 `240_add_intel_check_unverified_status.sql`。
- 配额迁移清理的是 `user_platform_quotas` 中的无限额度记录，不是用户表或使用记录表。OVH 发布前脚本统计待清理 `5128` 条；发布后 `user_platform_quotas` 为 `0` 行，`users` 为 `861` 行，`usage_logs` 为 `4105222` 行。
- 发布前 PostgreSQL 完整备份：`/opt/linkcode/deploy/backups/update-20260919-091610/postgres.sql`，SHA-256 记录在同目录 `postgres.sql.sha256`；同时保留 active/candidate Compose、环境文件和镜像回滚 tag。
- 本次正式发布未修改 DNS；Nginx 外部入口仍由现有线路接管，应用容器已完成正式版本切换。

## 10. 题库标准样本同步（2026-09-19）

- 对比新对话站与 OVH 正式环境的 `intel_check_questions` 后，确认题目「鹈鹕骑自行车」提示词、参考稿和绘图规则主体一致；新对话站记录 ID `1` 有 2 个 `drawing_rules.standard_sources`，OVH 记录 ID `2` 原为 0 个。
- 已从新对话站导出这 2 个标准样本，只更新 OVH 记录的 `drawing_rules.standard_sources`，没有覆盖题面、参考稿、判定规则或其他题目字段。
- 迁移后 OVH 记录为 2 个样本；两个样本长度分别为 `21064`、`16541` 字节，内容摘要与新对话站一致。应用和 motion sidecar 未重启，仍为 `healthy`、重启次数 `0`。
- 生产原题目行备份：`/opt/linkcode/deploy/backups/intel-check-sample-20260919-123213/question-2.before.json`；导入样本及 SHA-256 校验同目录保留。

## 11. 官方 v0.2.7 同步结果（2026-09-19）

- 官方标签：`v0.2.7`，提交 `aea725f2ea644d5592d0bbb1d63b607efa7e200a`；官方增量相对 v0.2.6 共 117 个文件，约 `+4921/-2616` 行。
- 官方新增能力已合入：Seedance / Ark 原生视频任务 API、插件宿主通用 KV 与只读状态通道、Antigravity Gemini thinking 变体、Gemini SSE 注释兼容、Kimi Coding Plan 403 限时暂停、Responses → Anthropic 根级 union tool schema 展平、DeepSeek Chat reasoning_content 回退。
- 官方标签内 `backend/cmd/server/VERSION` 仍为旧值 `0.2.5`；已按用户目标修正为 `0.2.7`，没有把上游过期版本号带入构建。
- 明确保留 LinkCode 个性化：Codex ticket 捕获/注入/fail-closed/审计脱敏、Codex 指纹与 compact 调度、IntelCheck/题库/motion、模型广场、SSO、支付、图片对象存储与代理、审计和 OVH 编排。官方删除的 Codex ticket 文件没有跟随删除。
- 冲突处理包括：恢复 OpenAI ticket harvest 专用 HTTP profile、保留 compact ticket 门控、补齐 Codex manifest 认证快照、补齐 Seedance APIKey capability、保留 Codex 图片 SSE `image_edit.*` 事件归一化，并将 root gateway 路由统一挂载模型白名单。
- 官方 v0.2.7 增量没有新增数据库迁移文件；本次没有执行迁移、发布、DNS 或服务器操作。
- 分支：`sync-official-v0.2.7`；基线：`a9a320fef`。当前代码尚未 push。

### 验证

- 后端全量编译：`go test -run '^$' ./...` 通过。
- 受影响后端包：`go test -tags=unit ./internal/service ./internal/repository ./internal/server/routes -count=1` 通过。
- 管理端/DTO：`go test -tags=unit ./internal/handler/admin ./internal/handler/dto -count=1` 通过。
- 前端：`pnpm --dir frontend typecheck` 和 `pnpm --dir frontend build` 通过；仅有既有 Vite 动态导入和大 chunk 警告。
- 仍存在一个基线失败：`TestOpenAIResponsesWebSocket_ChannelMappedTargetSelectsAccountWithoutRequestedAlias` 在同步前的 `a9a320fef` 上同样失败，属于既有 WS 渠道映射问题，未作为 v0.2.7 同步回归处理。

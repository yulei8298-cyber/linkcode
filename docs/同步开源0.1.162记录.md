---
title: '同步开源版本至 0.1.162'
type: 'chore'
created: '2026-07-21'
status: 'completed-uncommitted'
baseline_commit: 'd80a48a261cd2acbadfbc72bdf651e9a1caf3bee'
context:
  - 'docs/同步开源0.1.158记录.md'
  - 'docs/对话站每日免费分组开发记录.md'
  - 'docs/兑换码返利成本基数开发记录.md'
---

<frozen-after-approval reason="用户确认的同步目标与二开保护边界">

## 目标

**问题：** 当前项目基于开源 `v0.1.160`，需要同步 Wei-Shaw/sub2api 的 `v0.1.161`、`v0.1.162`，同时本地已有大量业务二开，不能直接覆盖。

**方案：** 以上游 `v0.1.160..v0.1.162` 的差异为输入，逐文件应用并处理冲突；上游通用修复优先接入，本地业务能力优先保留，冲突处采用兼容实现，无法兼容时舍弃对应上游改动。

## 边界与约束

**始终保证：** 保留 LinkCode 品牌、首页和 QQ 群展示、外链菜单新窗口、LobeHub SSO、对话站无密钥首聊建钥、隐藏/免费/每日额度/仅对话站分组、分组 IP ACL、免费分组零余额调用、Grok 对话/生图/视频兼容、兑换码返利成本基数，以及 Docker 构建内存和 Go 模块代理配置。

**需要暂停确认：** 只有在同步必须修改已执行迁移文件、覆盖生产数据、删除现有二开能力，或无法判断两种行为的业务优先级时暂停；普通代码冲突按“兼容优先、本地业务优先”自行处理。

**禁止事项：** 不修改已发布迁移的内容或 checksum；不执行生产部署或数据库操作；不提交、不推送；不为通过测试而删除二开测试或放宽鉴权、计费和来源校验。

## 场景矩阵

| 场景 | 输入或状态 | 预期行为 | 异常处理 |
| --- | --- | --- | --- |
| 上游普通文件 | 仅上游修改 | 完整同步 `v0.1.162` 版本 | 构建或测试失败则定位修复 |
| 双方同时修改 | 55 个已识别重叠文件 | 合并上游修复并保留本地行为 | 无法兼容时保留本地并记录舍弃项 |
| 迁移编号冲突 | 上游新增 `183/184`，本地已占用至 `186` | 上游迁移顺延为新的唯一编号 | 不覆盖或改名已上线迁移 |
| 对话站免费调用 | 隐藏免费分组、零余额、可信请求 | 可自动建钥并调用，按每日免费额度计费 | 非可信来源中文拒绝，额度耗尽中文提示 |
| 普通 API 调用 | 用户自建密钥或 Grok 媒体请求 | 保持现有鉴权、路由和计费 | 不产生无密钥回归或错误路由 |

</frozen-after-approval>

## 代码范围

- `backend/internal/server/middleware/`、`backend/internal/service/api_key*` -- 上游鉴权缓存与入口安全改动和本地对话站、免费分组、IP ACL 的主要冲突区。
- `backend/internal/service/openai_*`、`backend/internal/handler/grok_media.go` -- 上游 OpenAI/Grok 修复与本地媒体路由兼容区。
- `backend/internal/config/`、`backend/internal/handler/admin/setting_*` -- 新客户端 IP、安全开关、图片存储设置与本地配置项合并区。
- `backend/migrations/` -- 上游新迁移顺延、本地 `177`、`183..186` 保持不变。
- `frontend/src/` -- 上游管理页面和国际化修复，同时保护本地首页、页头、侧边栏和 API 密钥提示。
- `Dockerfile`、`deploy/` -- 接入上游构建与部署修复，同时保留生产构建参数。

## 任务与验收

**执行任务：**

- [x] 应用 `v0.1.160..v0.1.162` 上游差异，处理新增、修改、删除文件及二进制资源。
- [x] 逐项解决重叠文件冲突，补齐必要的 Ent/Wire 生成代码并保留所有二开字段和入口。
- [x] 将上游新迁移映射到本地未占用编号，增加迁移顺序与内容回归检查。
- [x] 执行后端单元测试、前端类型检查/测试/构建、嵌入式后端构建和差异检查。
- [x] 在本文档追加实际同步范围、冲突取舍、迁移映射、测试结果和未提交状态。

**验收标准：**

- Given 当前二开代码，when 同步结束，then 服务版本为 `0.1.162` 且上游两版的通用修复已进入工作区。
- Given 对话站免费分组和零余额用户，when 发起可信首个请求，then 仍可自动建钥、返回结果并执行每日额度控制。
- Given 普通或 Grok API 调用，when 使用现有密钥调用对话、生图或视频，then 鉴权、路由、计费与同步前兼容。
- Given 数据库已执行本地 `183..186`，when 新版本启动迁移，then 只执行顺延后的上游新迁移且不发生 checksum 冲突。
- Given 全部验证完成，when 查看 Git 状态，then 仅保留本次同步和文档改动，且没有新提交或推送。

## 变更记录

### 同步方法与回退点

- 同步前提交为 `d80a48a261cd2acbadfbc72bdf651e9a1caf3bee`，已建立本地回退分支 `backup/pre-sync-v0.1.162-20260721`。
- 因本地仓库与上游无共同祖先，使用上游标签 `v0.1.160..v0.1.162` 的树差异通过三方补丁应用，没有合并上游 Git 历史。
- 上游标签 `v0.1.162` 内的 `cmd/server/VERSION` 仍误写为 `0.1.161`，本地明确修正为 `0.1.162`；`scripts/resolve-version.sh` 验证结果也是 `0.1.162`。
- 最终提交相对同步前基线涉及 381 个文件（新增 61、修改 318、删除 2）。统计包含上游同步、冲突兼容、测试修正和本文档；已舍弃的上游 README Logo 不计入最终结果。

### 冲突与二开兼容

共处理 10 个文本冲突，最终不存在未合并文件或冲突标记。主要取舍如下：

- **品牌与页面：** 保留 LinkCode 首页、默认站点名与副标题、QQ 群 `1025176993`、页头和侧边栏入口、LobeHub SSO，以及自定义菜单在新窗口打开。删除已被 LinkCode `frontend/public/logo.svg` 替代的旧 Sub2API `logo.png`，不引入上游 README Logo 资源。
- **鉴权与免费分组：** 合并上游超长密钥拒绝、入口拒绝聚合、鉴权缓存失效机制；同时保留隐藏分组、免费分组、每日额度、仅对话站调用、无密钥首聊自动建钥、分组 IP ACL，以及免费分组在用户余额为零时仍可调用的本地逻辑。
- **敏感字段边界：** 全量契约测试发现合并后的普通用户分组接口会返回 `ip_whitelist`、`ip_blacklist`。已将字段从公共 `dto.Group` 移至管理员专用 `dto.AdminGroup`，管理员配置不受影响，普通用户不再看到分组 ACL。
- **OpenAI/Grok 路由：** 保留根路径、`/v1`、`/v1/v1` 三套兼容入口，以及 Grok 对话、生图、视频和 `/v1/videos/generations`；接入上游 count-tokens、媒体内容下载、请求体限制和 Grok 缓存改进。
- **系统设置：** 保留 LinkCode 的 `chat_station_url`、`pricing_display_config`、`tutorial_content_md` 和默认品牌值；接入上游自定义客户端 IP Header，并继续保持转发 IP 信任默认关闭。
- **管理更新：** 保留上游在线更新/回退请求的 15 分钟超时，修正本地旧测试断言，避免慢速 GitHub 下载被全局 30 秒超时中断。
- **Wire：** 执行 `go generate ./cmd/server` 重新生成依赖注入代码，结果与合并后的服务构造参数一致；本次没有 Ent schema 变化，因此未重生成 Ent。

### 数据库迁移

- 本地已发布迁移 `183_prompt_audit.sql`、`184_prompt_audit_full_prompt.sql`、`185_redeem_code_affiliate_rebate_base.sql`、`186_backfill_historical_redeem_code_rebate_bases.sql` 保持文件名和内容不变。
- 上游 `183_ops_ingress_reject_aggregates.sql` 顺延为 `187_ops_ingress_reject_aggregates.sql`。
- 上游 `184_auth_cache_invalidation_outbox.sql` 顺延为 `188_auth_cache_invalidation_outbox.sql`。
- `188` 的触发字段在上游基础上兼容本地 `is_hidden`、`is_free`、`daily_free_limit_usd`、`chat_station_only`、分组 IP 白名单和黑名单，确保这些权限变化会主动失效鉴权缓存。
- 新增 `upstream_v0162_migration_test.go` 检查顺延文件存在、旧编号不被覆盖及关键 SQL 内容。应用启动时迁移执行器会自动运行新增迁移，不需要手工 SQL。

### 生产构建入口

- 服务器的 `deploy/update.sh` 实际使用 `deploy/Dockerfile` 和 `deploy/docker-compose.build.yml`。已把上游 BuildKit pnpm/Go 缓存、原生架构构建、目标架构交叉编译和 PostgreSQL 客户端打包同步到该二开入口。
- 保留 `NODE_OPTIONS=--max-old-space-size=4096`、pnpm `9.15.9`、官方 Go 模块代理、LobeHub 环境变量、服务端口和原有 Docker 数据卷。
- 同步 PostgreSQL 可调参数、`UPDATE_GITHUB_TOKEN` 和 Redis 多行命令续行修复。`docker compose ... config -q` 校验通过；本机 Docker daemon 未启动，因此没有执行镜像级 `docker buildx --check`，但前后端生产构建已分别验证通过。

## 设计说明

本地 Git 历史与上游没有共同祖先，但本地树已同步到 `v0.1.160`。因此以两个上游标签间的树差异作为补丁，比直接合并无关历史更可控。上游新增迁移只改本地落地文件名，不改 SQL 语义；编号从本地最后一个迁移之后继续排列。

## 验证

**命令：**

- `GOCACHE=/tmp/linkcode-go-build-v0162 go test -tags=unit ./...`（backend）-- 全量通过；包含 middleware、routes、service、repository、migrations 和 API 契约测试。
- `pnpm typecheck` -- 通过。
- `pnpm test:run` -- 178 个测试文件、1242 项测试全部通过。
- `NODE_OPTIONS=--max-old-space-size=4096 pnpm build` -- 生产前端构建通过，963 个模块完成转换；仅有既存的动态/静态导入和大 chunk 警告。
- `CGO_ENABLED=0 go build -tags embed -o /tmp/linkcode-sub2api-v0.1.162 ./cmd/server` -- 嵌入前端的服务端二进制构建通过。
- `docker compose -f deploy/docker-compose.build.yml --env-file deploy/.env config -q` -- 自建发布 Compose 配置通过。
- `git diff --check`、冲突标记扫描、`git status` -- 无格式错误、无未解决冲突。

## 当前状态

- 同步、兼容和验证已完成。
- 按用户要求，没有创建 Git 提交、没有推送、没有连接或部署生产服务器、没有执行生产数据库操作。
- 上游补丁应用阶段产生的暂存状态已统一取消，最终改动全部保留在工作区，便于提交前继续检查。

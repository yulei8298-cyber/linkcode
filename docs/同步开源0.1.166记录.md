# 同步开源 0.1.166 记录

## 一、同步信息

- 同步日期：2026-07-27
- 本地仓库：`linkcode`
- 当前分支：`main`
- 同步前提交：`8af0a7b58bb6dc338cbe0e5c8baf9418194ba3a4`
- 同步前版本：`0.1.162`
- 上游仓库：`Wei-Shaw/sub2api`
- 上游目标标签：`v0.1.166`
- 上游目标提交：`dc893dd0b8eab41df5be595ae9fcd1aa74a062b8`
- 上游提交数量：`v0.1.162..v0.1.166` 共 228 个提交
- 上游原始差异：507 个文件，39358 行新增，1766 行删除
- 同步前回退分支：`backup/pre-sync-v0.1.166-20260727`
- 本次状态：未提交、未推送、未部署

> 上游 `v0.1.166` 标签中的 `backend/cmd/server/VERSION` 仍为 `0.1.165`，本次已按目标标签明确修正为 `0.1.166`。

## 二、同步原则

1. 以 `v0.1.162..v0.1.166` 的完整上游差异为同步来源。
2. 本地已发布迁移不可改名、不可覆盖、不可修改内容。
3. 业务冲突优先合并双方能力；无法兼容时保留本地二开。
4. 品牌、对话站、免费分组、Grok 兼容路由、返利成本和自建发布流程优先级高于上游非核心改动。
5. 生成代码必须根据合并后的 Schema/Wire 重新生成，不手工长期维护冲突产物。

## 三、引入的主要上游能力

- Composite 聚合分组、模型路由、模型别名和路由预览。
- OpenAI/Codex 分组级 reasoning effort 上限和映射。
- ChatGPT Live 网关、组级 Live 开关、并发租约和 Live 用量类型。
- Anthropic Claude Opus 5 支持。
- Ollama Cloud 用量刷新。
- 面板 API 限流。
- Redis ACL username。
- 会话标识 `session_id` 用量记录。
- 支付宝移动端深链。
- OpenAI、Grok、Gemini、Antigravity、WebSocket、SSE、计费和统计修复。
- 前端移动端适配及 axios、postcss 等安全依赖升级。

## 四、冲突处理

本次共处理 17 个显式 Git 冲突文件：

| 区域 | 处理结果 |
| --- | --- |
| `README.md`、`README_JA.md` | 保留本地合作方内容，同时接收上游功能概览；未用上游合作方内容覆盖本地内容。 |
| `backend/cmd/server/VERSION` | 舍弃标签内滞后的 `0.1.165`，设置为 `0.1.166`。 |
| Ent 生成文件 | 先合并 Group Schema，再执行 `go generate ./ent`，同时生成本地免费分组字段和上游 Live/推理策略字段。 |
| 分组请求 DTO | 同时保留 `is_hidden`、`is_free`、`daily_free_limit_usd`、`chat_station_only`、IP ACL，并接入 `composite` 平台。 |
| 分组业务服务 | 同时保留免费额度校验和上游 Composite 账号复制、reasoning effort 校验。 |
| API Key 鉴权缓存 | 快照版本提升至 18，同时覆盖免费/对话站字段和上游 Live 字段。 |
| 主路由 | 保留公开接口和 LobeHub SSO，接入上游 Panel API 限流及 Composite Resolver。 |
| 网关路由 | 保留根路径及 `/v1/v1` 兼容、Grok 生图/视频路由；所有模型请求接入 Composite 目标解析。 |
| 网关测试 | 同时保留本地兼容路径测试和上游 Composite endpoint 测试。 |
| AppHeader | 保留 QQ 群 `1025176993`，合并上游窄屏布局。 |
| GroupsView | 免费分组校验和 reasoning effort 校验依次执行；Live 能力接口不存在时安全降级，兼容滚动发布和旧测试 Mock。 |
| CC Switch | 保留本地 Base URL 归一化，并加入 Grok Build 导入。 |
| MonitorTimeline | 接收上游窄卡片溢出修复。 |

素材处理：

- 保留本地 `assets/partners/logos/apikl.png`，不接受上游同名二进制覆盖。
- 保留仍被本地 README 引用的 `assets/partners/logos/tokeneum.png`。
- 接收上游删除的未引用旧素材 `byteplus.png`、`huoshan.png`、`miyaip.png`。

## 五、迁移兼容

本地生产迁移已使用到 `188`。上游迁移与本地 `172/185/186/187/188` 撞号，因此没有覆盖任何旧文件，统一顺延：

| 上游文件 | 本地同步后文件 |
| --- | --- |
| `172_composite_model_routes.sql` | `189_composite_model_routes.sql` |
| `185_group_reasoning_effort_policy.sql` | `190_group_reasoning_effort_policy.sql` |
| `186_alipay_mobile_precreate_deep_link.sql` | `191_alipay_mobile_precreate_deep_link.sql` |
| `187_add_usage_log_session_id.sql` | `192_add_usage_log_session_id.sql` |
| `188_allow_live_usage_request_type.sql` | `193_allow_live_usage_request_type.sql` |
| `189_add_group_allow_live.sql` | `194_add_group_allow_live.sql` |
| `190_add_users_email_alias_dedup_index_notx.sql` | `195_add_users_email_alias_dedup_index_notx.sql` |
| `186_group_auth_cache_image_generation.sql` | `196_extend_group_auth_cache_invalidation.sql` |

`196` 放在相关字段创建之后执行，并合并了鉴权缓存失效条件：

- 本地字段：隐藏、免费、每日免费额度、仅对话站、分组 IP 白名单/黑名单。
- 上游字段：生图权限、Live 权限、reasoning effort 上限和映射。

新增 `upstream_v0166_migration_test.go`，用于防止后续再次引入撞号文件或遗漏鉴权字段。

## 六、保留的二开能力

- LinkCode 品牌、站点文案和默认教程。
- QQ 群 `1025176993`。
- 自定义菜单新窗口打开。
- LobeHub SSO。
- 对话站无密钥首聊自动创建隐藏密钥。
- 隐藏分组、免费分组、每日免费额度、仅对话站调用。
- 仅免费分组可在用户余额为零时继续调用；普通余额分组仍校验余额。
- 免费额度用完返回中文提示：`当前每日免费额度已用完，请明天再使用`。
- 分组 IP 白名单/黑名单及管理员专属 DTO。
- Grok 对话、生图、视频和 `/v1/videos/generations`。
- 根路径、`/v1`、`/v1/v1` 客户端兼容路由。
- 兑换码返利成本基数和历史档位。
- 提示词审计、入口拒绝聚合、跨实例鉴权缓存失效。
- 自建发布入口 `deploy/Dockerfile`、`deploy/docker-compose.build.yml`、`deploy/update.sh`。
- Docker 前端构建 `NODE_OPTIONS=--max-old-space-size=4096` 和 pnpm `9.15.9`。

## 七、验证结果

以下验证均已通过：

```text
go generate ./ent
go generate ./cmd/server
go test -tags=unit ./...
pnpm 9.15.9 install --frozen-lockfile
pnpm 9.15.9 typecheck
pnpm 9.15.9 test:run
NODE_OPTIONS=--max-old-space-size=4096 pnpm 9.15.9 build
CGO_ENABLED=0 go build -tags embed ./cmd/server
docker compose -f deploy/docker-compose.build.yml --env-file deploy/.env config -q
git diff --check
```

测试结果：

- 后端全部单元测试通过。
- 前端 192 个测试文件、1331 条测试全部通过。
- 前端生产构建通过。
- 后端嵌入前端后的静态二进制构建通过，启动输出版本 `0.1.166`。
- 自建 Docker Compose 配置解析通过。
- 迁移顺序和鉴权字段回归测试通过。

非阻断提示：

- Vite 仍有既有的大 Chunk 和动态/静态重复导入警告。
- Browserslist 数据提示可更新，但不影响构建。
- 本次未连接生产 PostgreSQL，未执行生产迁移，也未启动或重启生产服务。

## 八、最终工作区

- 所有同步内容保留在工作区。
- 未创建本次同步提交。
- 未推送远端。
- 未部署服务器。
- 如需对比同步前状态，可使用回退分支 `backup/pre-sync-v0.1.166-20260727`。

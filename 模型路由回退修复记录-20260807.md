# 模型路由回退修复记录

## 问题

OpenAI 分组配置模型路由后，匹配模型只允许使用指定账号。指定账号不可用时，调度器会直接返回无可用账号，无法使用同分组的普通账号继续请求。

## 修改内容

- 匹配模型优先选择配置的路由账号。
- 路由账号不可用、限流、并发已满或模型不兼容时，回退到同分组的普通账号。
- 其他模型仍不能使用已被模型路由规则保留的账号。
- 调整粘性会话和历史响应账号选择，避免正常账号的旧绑定绕过匹配模型的优先路由。
- 更新中英文管理页面提示，明确“优先路由，异常时回退”的行为。

## 生产配置注意事项

以 `gpt-5.6-luna -> 331` 为例：

1. 开启目标分组的模型路由开关。
2. 将账号 331 加入目标分组，否则该账号不在该分组候选池中，只能触发普通账号回退。
3. 保存配置并重新构建发布后，目标模型会优先使用 331；331 不可用时自动使用普通账号。

本次仅修改代码和文档，没有修改生产数据库、配置或服务状态。

## 验证

- `go test ./internal/service -run 'TestOpenAI(ModelRouting|AccountSchedulerModelRouting)' -count=1`
- `go test ./internal/service -count=1`

以上测试均通过。

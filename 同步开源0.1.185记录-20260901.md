# 同步开源 0.1.185 兼容二开记录

## 1. 同步结论

- 同步日期：2026 年 9 月 1 日
- 开源仓库：`Wei-Shaw/sub2api`
- 目标版本：`v0.1.185`
- 目标提交：`2ac784c51a5d0925b324efef2ba6b3446c364781`
- 当前二开基线：`1b624537e83a3dde403126c007ee42aa54845b7b`
- 同步结果：官方增量已应用到当前二开工作区，未创建提交、未推送、未部署生产。

由于当前二开仓库与官方仓库历史没有可直接合并的共同祖先，本次采用官方
`v0.1.183 -> v0.1.185` 增量补丁，并通过 `git apply --3way` 应用，避免以官方
文件整体覆盖二开代码。

## 2. 冲突处理原则

冲突处理统一遵循“保留现有二开行为，吸收不冲突的官方修复”的原则：

| 文件 | 处理结果 |
| --- | --- |
| `README.md`、`README_JA.md` | 保留二开赞助商及项目说明内容，未用官方版本覆盖 |
| `backend/cmd/server/VERSION` | 手动设置为 `0.1.185`；官方标签文件内容仍为 `0.1.184`，以目标标签为准 |
| `backend/internal/server/routes/gateway.go` | 保留二开 OpenAI 平台判断、自定义 Codex 模型分发；未采用官方对所有 `client_version` 请求无条件切换 Codex manifest 的逻辑 |
| `backend/internal/service/admin_group.go` | 保留隐藏/免费组、每日免费额度、Chat Station、IP 黑白名单等二开字段；吸收官方对 `DailyLimitUSD`、`WeeklyLimitUSD`、`MonthlyLimitUSD` 为 `nil` 时不修改原值的局部更新语义 |
| `backend/internal/service/openai_gateway_passthrough.go` | 保留结构化进度、首输出超时保护、SSE keepalive、失败转移和二开命名空间处理；补回官方可见输出 TTFT 分类逻辑，并统一 native/passthrough TTFT 判断 |
| `backend/internal/service/openai_gateway_response_handling.go` | 保留二开结构化进度分类，但空的 reasoning/output-item 元数据不会解除首输出超时保护；只有真实客户端可见输出才会结束首输出保护 |
| `backend/internal/service/openai_visible_ttft_test.go` | 保留官方文本/工具参数/图片可见输出测试，并保留二开结构化进度与首输出超时回归测试 |

## 3. 保留的二开能力

- 无限画布、prompt audit / security audit 及相关路由和资源未被删除。
- OpenAI Responses、SSE、WebSocket、HTTP/2/fallback 等现有兼容逻辑继续保留。
- 部署侧现有 512 MB 请求体限制、OpenAI HTTP/2 与 HTTP/1.1 fallback 配置保持不变；`deploy/.env.example` 与 `deploy/docker-compose.build.yml` 的这些工作区改动仍保持未暂存。
- OVH/线路机发布所需的部署文件没有被本次代码同步覆盖。

## 4. 开源 0.1.185 主要吸收内容

- 价格目录支持 `pricing.override_file` JSON 覆盖，并改进长上下文阶梯计价。
- Codex/OpenAI 网关增强：priority service tier、模型能力目录、Responses/WebSocket 稳定性和 delegation bootstrap 兼容。
- 账号统计与 DeepSeek 峰谷价格计算优化。
- 数据库启动瞬时错误重试、OpenAI WebSocket 连接池回收、容量降载错误处理等稳定性修复。
- 新增 OpenAI reasoning effort、native compaction v2、用户公共组限制等数据字段及配套代码/测试。
- 新增上游计费探测、OpenAI/Codex 模型元数据、图片工具冷却、非流式终止失败转移、SSE keepalive 等测试覆盖。

## 5. 数据库迁移注意事项

本次新增迁移文件：

- `backend/migrations/231_add_usage_log_native_compaction_v2.sql`
- `backend/migrations/231_add_usage_log_requested_reasoning_effort.sql`
- `backend/migrations/231_user_restrict_public_groups.sql`

这些迁移已随代码进入工作区，但“代码同步”不等于“立即执行生产数据库迁移”。发布到 OVH 或生产前，应先确认数据库备份、迁移执行顺序、回滚方案以及现有二开迁移编号没有冲突，再单独执行迁移。

## 6. 验证结果

- `git diff --cached --check`：通过。
- 后端 `go test ./...`：通过，所有 Go 包均为 `ok`。
- 前端 `pnpm run typecheck`：通过。
- 前端仅针对源码执行 `pnpm exec eslint src --ext .vue,.js,.jsx,.cjs,.mjs,.ts,.tsx,.cts,.mts`：通过。
- 前端仓库级 `pnpm run lint:check`：未通过，原因是仓库原有的 `frontend/public/infinite-canvas` 压缩产物被 ESLint 扫描，产生大量压缩代码规则错误；不是本次 `src` 同步代码的错误，未修改该第三方/构建产物。
- 同步后曾发现 TTFT 合并遗漏（缺少可见输出分类函数、结构化元数据错误解除超时），已修正，并通过对应回归测试及后端全量测试。

## 7. 当前工作区与后续建议

- 同步变更目前处于 staged 状态，方便后续人工审阅；没有执行 `git commit`、`git push`。
- 用户原有的 `deploy/.env.example`、`deploy/docker-compose.build.yml` 修改保持 unstaged；原有记忆/迁移记录文件保持不动。
- 在提交前建议人工审阅 `git diff --cached`，重点检查网关路由、账号组局部更新、Responses TTFT、数据库迁移和部署配置。
- 发布到 OVH 前建议先在独立环境构建镜像并执行迁移演练，再进行灰度/平滑切换；本次操作没有连接 OVH、没有修改 DNS、Redis 或生产数据库。

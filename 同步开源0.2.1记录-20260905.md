# 同步开源 0.2.1 记录

日期：2026-09-06

## 目标与基线

- 上游仓库：`Wei-Shaw/sub2api`
- 目标标签：`v0.2.1`
- 上游提交：`578785ee7fb35030b094b69624efe25670a36f5f`
- 本地基线：`dc0600fc81e27bbba2bee11bbaafb4feee4842fa`（LinkCode 二开 v0.2.0）
- 同步范围：上游 v0.2.0 到 v0.2.1，82 个提交、297 个文件，约新增 12,622 行、删除 1,033 行。

## 合并策略

本次在临时隔离工作树完成三方合并，再将最终补丁应用回主工作区。保留 LinkCode 二开行为和用户已有未提交文件；代码冲突以本地业务行为为优先，同时补入上游新增能力。未执行提交、推送、部署、DNS 切换或数据库迁移。

## 主要兼容处理

- Ent Group schema 同时保留免费组、隐藏组、Chat Station、IP ACL、利润控制、OpenAI Fast/推理策略，并加入上游 `codex_models_manifest_config`；随后重新生成 Ent 代码，避免手工索引错位。
- API Key 鉴权快照版本从 22 升为 23，保留本地授权、定价和 Fast/推理字段，并纳入 Codex Manifest 字段。
- OpenAI WebSocket cyber 风控同时保留本地逐 turn 输入证据记录，以及上游 failover 后的 blocked/pending 状态推进和会话清理。
- 保留本地管理端字段（免费/隐藏分组、IP 白/黑名单、返利基础额），并接入上游紧凑账户列表、用量 upstream request ID、Codex Manifest DTO。
- delegation/automation bootstrap 兼容历史上下文和安全 heartbeat XML 校验；Responses 流式错误保留 gateway 错误码。
- README 保留 LinkCode 现有赞助商和品牌内容；恢复上游删除但本地仍使用的 `novada.png`、`sui-xiang.jpg`。

## 上游新增能力

已同步 GPT-6 Astra、Codex Ultrafast、固定账号 Model Manifest、图片 URL 安全回填、上游请求 ID、reasoning 计费倍率、定价热加载、会话/WS 修复、OpenCode/Gemini 能力更新、管理端紧凑账户列表和支付宝订单对账等代码及测试。

## 数据库迁移注意事项

上游新增以下迁移文件，已保留但本次不执行：

- `232_add_usage_log_upstream_request_id.sql`
- `233_add_usage_log_upstream_request_id_index_notx.sql`（`CREATE INDEX CONCURRENTLY`，必须走非事务迁移）
- `234_channel_max_reasoning_effort_multiplier.sql`
- `234_group_codex_models_manifest_config.sql`

存在两个 `234` 前缀文件。当前迁移器按完整文件名跟踪，技术上不会覆盖，但上线前应在 staging 演练、备份数据库并确认迁移顺序；已执行迁移不得改名或改内容。

## 验证结果

- `go generate ./ent`：通过。
- `go test ./cmd/server`：通过。
- automation heartbeat、delegation 兼容定向测试：通过。
- `git diff --check`：通过，未发现冲突标记。
- 完整 `go test ./...`：主体包通过；`internal/handler` 仍有少数历史测试预期与本地二开/上游新语义不一致，已记录为后续 review 项，不影响 server 编译。

## 当前状态

- 同步补丁已应用到主工作区。
- 未提交、未 push、未部署，生产服务未重启。
- 用户已有改动保持原样：`deploy/.env.example`、`deploy/docker-compose.build.yml` 以及未跟踪 memory 文件。
- `backend/cmd/server/VERSION` 继续保持 `0.2.0`：官方 `v0.2.1` 标签内嵌版本同样为 `0.2.0`，本次未擅自修改服务版本号。

# 同步开源 0.1.172 记录

## 同步范围

- 本地基线：`0.1.169`（LinkCode 二开基线提交 `eba58f47c`）
- 上游目标：`Wei-Shaw/sub2api` 标签 `v0.1.172`
- 同步方式：将 `v0.1.169..v0.1.172` 的上游变更应用到本地二开基线，并逐项处理冲突。
- 本次结果仅保留为工作区修改，未执行 `git commit`、`git push` 或任何服务器操作。

## 已同步的上游能力

本次纳入了上游在 0.1.170 至 0.1.172 的主要更新，包括：

- OpenAI/Codex 身份、工具 schema、`count_tokens`、WebSocket 和容量降载修复。
- OAuth 登录流程安全修复、Passkey、腾讯天御及阿里云人机验证支持。
- 分组利润控制、账号调度利润校验及预览能力。
- 上游响应模型审计、用量筛选与对应索引。
- 订阅每日额度重置、网络连接超时、Grok 405 故障转移、金额精度量化和系统日志退避。
- 模型广场组合分组展示、Antigravity Gemini 3.6 Flash 等模型兼容更新。

## 二开兼容处理

1. 保留 LinkCode 的公开门户首页、对话站入口、QQ群展示、定价展示和教程入口。上游“紧凑首页”不覆盖现有 `HomeView.vue`，但其配置字段及管理端开关已保留，避免后续升级再次冲突。
2. 系统设置同时保留本地 `pricing_display_config`、`tutorial_content_md`、`chat_station_url`，并合入上游紧凑首页、腾讯验证码和阿里云验证码字段；后台读取、保存、公开接口、审计记录、前端类型和表单均已同步。
3. 分组继续保留隐藏分组、免费分组/每日免费额度、仅对话站调用、分组 IP 白黑名单，并合入利润控制字段。API Key 鉴权快照、缓存恢复和数据库失效触发器均包含两类字段。
4. 使用合并后的 Ent schema 重新生成 ORM 文件，确保免费分组字段与利润控制字段同时存在。
5. 保留本地公开定价与 LobeHub SSO 处理器。上游 `wire_gen.go` 缺少两个本地处理器参数，已按当前 `wire.go` 重新生成，且编译验证通过。
6. 保留本地 OAuth `count_tokens` 的 404 本地估算逻辑，并合入上游 HTML 403 代理页处理。该请求是可选的 token 估算能力，不应因上游 endpoint 不可用而把账号标记为异常。

## 数据库迁移处理

本地生产库已使用 `192` 至 `197` 号迁移，上游新增迁移原本也占用 `192` 至 `195`，不能直接合并。已调整为：

| 最终文件 | 内容 |
| --- | --- |
| `198_group_profit_control.sql` | 分组利润控制字段 |
| `199_group_profit_control_auth_cache_invalidation.sql` | 分组鉴权缓存失效触发器扩展 |
| `200_add_usage_log_upstream_response_model.sql` | 上游响应模型审计字段 |
| `201_add_usage_log_upstream_model_mismatch_index_notx.sql` | 上游模型不一致索引 |

同时已更新非事务索引迁移清理逻辑，使 `201` 号并发索引迁移失败后可以安全重试。

## 验证结果

- `go generate ./ent`：通过。
- `go generate ./cmd/server`：通过。
- `go test ./internal/service ./internal/repository ./internal/handler ./internal/server/... -run '^$' -count=1`：通过编译校验。
- `go test ./internal/service -count=1`：通过。
- `go test ./cmd/server -run '^$' -count=1`：通过。
- `vue-tsc --noEmit`：通过。
- 设置、分组利润控制、推理策略、公开设置缓存、验证码登录相关 Vitest：5 个文件、81 项通过。
- `NODE_OPTIONS=--max-old-space-size=4096 pnpm build`：通过。
- `CGO_ENABLED=0 go build -tags embed -trimpath ./cmd/server`：通过。

## 发布注意事项

- 本次包含新迁移，应用启动时会由现有迁移执行器按文件名顺序执行 `198` 至 `201`。
- 当前工作区刻意保持未提交状态；在人工检查差异后再决定提交与发布。

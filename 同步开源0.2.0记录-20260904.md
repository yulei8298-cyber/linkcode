# 同步开源 0.2.0 兼容二开记录

## 1. 同步结论

- 同步日期：2026 年 9 月 4 日
- 开源仓库：`Wei-Shaw/sub2api`
- 起始版本：`v0.1.185`，提交 `2ac784c51a5d0925b324efef2ba6b3446c364781`
- 目标版本：`v0.2.0`，提交 `aa236488351eb71e120fc2b6fb32e36b0374c918`
- 增量规模：约 60 个提交、148 个文件，最终同步变更约 145 个文件（新增/修改约 4717 行，删除约 534 行）。
- 同步结果：目标版本代码已应用到当前二开工作区，版本文件已调整为 `0.2.0`；未提交、未推送、未部署生产，也未执行数据库迁移。

当前二开仓库与开源仓库没有共同 merge-base，因此没有直接执行普通 merge。同步过程使用
`v0.1.185 -> v0.2.0` 的增量补丁，在临时隔离工作树中合并后再应用回主工作区，避免以开源文件整体覆盖二开实现。

## 2. 冲突处理原则

统一遵循“本地二开行为优先，吸收不冲突的上游修复；无法同时保留时舍弃上游会破坏现网行为的部分”的原则。

重点处理如下：

- Ent Group Schema 合并本地字段（隐藏/免费组、每日免费额度、Chat Station、IP ACL 等）与上游字段（OpenAI Fast、reasoning 超限策略），并重新生成 Ent 代码及字段索引。
- API Key 鉴权快照版本从 `20` 升为 `22`，保留免费组、IP ACL、利润控制等二开快照字段，同时加入 Fast 与 reasoning 策略。
- 管理端 Group DTO 同时支持 `force_openai_fast`、`free_openai_fast`、`max_reasoning_effort_over_limit`。
- 管理端分组页面补齐 reasoning 超限策略的创建、编辑、默认值和提交绑定；恢复模型定价按钮的响应式布局 class。
- 保留此前 Grok 媒体转发 Content-Type 修复（提交 `4c06d5a17`），以及本地 API-fast、无限画布和部署参数。
- 未覆盖用户现有未提交文件：`deploy/.env.example`、`deploy/docker-compose.build.yml`，以及工作区中的迁移记忆文件。

## 3. 吸收的开源 0.2.0 能力

- OpenAI Fast 分组策略，及免费 Fast 请求按 Standard 价格计费。
- 按模型配置 OpenAI reasoning effort，并支持超限 `deny` / `downgrade`。
- Kimi 原生 Responses API 转发。
- Claude Fable 5.1 支持。
- 1 小时缓存写入价格及相关价格解析、账号统计优化。
- OpenAI Responses/WebSocket、Anthropic fallback、自动化启动请求、模型冷却和容量降载等稳定性修复。
- 管理端分组模型价格及 reasoning/Fast 配置界面、DTO 和回归测试。

## 4. 新增数据库迁移（尚未执行）

- `backend/migrations/232_channel_cache_write_1h_pricing.sql`
- `backend/migrations/232_group_force_openai_fast.sql`
- `backend/migrations/232_group_reasoning_effort_over_limit.sql`
- `backend/migrations/233_group_free_openai_fast.sql`

这些文件仅随代码同步进入工作区。上线前必须先完成数据库备份、迁移编号冲突检查、执行顺序确认和回滚预案；本次不会自动修改 OVH 或生产数据库。

## 5. 验证结果

- `git ls-files -u`：无未解决冲突。
- `git diff --cached --check`：通过。
- 新增 Group DTO、Fast 策略、reasoning 策略、API Key 鉴权快照和 Ent 相关回归测试已通过。
- 后端全量测试中发现的本地旧快照版本断言已更新为 `22`，相关测试重新通过。
- 后端 `go test ./...`：通过；后端 `go test -tags=unit ./...`：通过。
- 前端源码 ESLint、`pnpm run typecheck`、`pnpm run test:run`（253 个测试文件、1847 项）和 `pnpm run build`：通过；仓库级 `lint:check` 仍会扫描既有的 `frontend/public/infinite-canvas` 压缩产物并报原有规则错误，未修改该构建产物。

## 6. 当前状态与发布注意事项

- 同步文件当前保持 staged，便于人工审阅；没有执行 `git commit` 或 `git push`。
- 没有连接 OVH、没有修改 DNS、没有切换流量、没有重启生产服务。
- 发布前建议重点审阅网关路由、Group 字段局部更新、API Key 快照、Responses/WebSocket、数据库迁移和部署配置；确认迁移演练及回滚方案后再单独发布。
